% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit-ambient.R
\name{audit_record}
\alias{audit_record}
\title{Record Data-Frame Lineage for a Block of Code}
\usage{
audit_record(
  expr,
  name = NULL,
  env = parent.frame(),
  watch = "data.frames",
  ignore = NULL,
  level = c("metadata", "sample_hash", "column_hash", "full_hash"),
  keys = NULL,
  numeric_summary = TRUE,
  continue_on_error = FALSE
)
}
\arguments{
\item{expr}{A braced block of statements, e.g. \code{{ x <- ...; y <- ... }}.
Captured unevaluated and evaluated statement by statement in \code{env}.}

\item{name}{Optional trail name. If \code{NULL}, a timestamped name is generated.}

\item{env}{Environment in which to evaluate the block. Defaults to the
caller's environment.}

\item{watch}{Either \code{"data.frames"} (the default — track every data.frame)
or a character vector of object names to restrict tracking to.}

\item{ignore}{Optional character vector of regular expressions; objects whose
names match any pattern are skipped (e.g. scratch variables).}

\item{level}{Evidence level: \code{"metadata"} (default, privacy-safe) detects
shape/type/NA changes only; \code{"sample_hash"}, \code{"column_hash"}, and
\code{"full_hash"} additionally detect value-only changes by hashing data with a
per-run salt. Salted hashes are \emph{not} a privacy guarantee. The hashing
policy (algorithm, sampling, salt) is recorded in each snapshot's
\code{evidence} field.}

\item{keys}{Optional named list mapping object names to key column(s), used by
the HTML report to flag primary-key status.}

\item{numeric_summary}{Logical; passed to the snapshot builder. If \code{FALSE},
skip numeric quantile summaries (the main cost control on wide data).}

\item{continue_on_error}{Logical. If \code{FALSE} (default), an error in a
statement is recorded and then re-thrown so the block aborts like normal R
evaluation. If \code{TRUE}, the error is recorded and evaluation continues.}
}
\value{
An \code{\link[=audit_trail]{audit_trail()}} populated with versioned, lineage-aware snapshots.
}
\description{
Evaluates a block of top-level statements and records a versioned audit trail
of every data.frame created or changed along the way — without per-step taps.
Capture granularity is \strong{top-level statement lineage}: a multi-verb pipe
assigned in one statement is a single step, and a loop yields one snapshot
after it. For intra-pipeline detail, use the explicit taps (\code{\link[=audit_tap]{audit_tap()}},
\verb{*_join_tap()}, \code{\link[=filter_tap]{filter_tap()}}), which compose inside an audited run.
}
\details{
Capture is metadata-only by default (shape, types, NA counts); raw rows never
enter the trail unless a hash \code{level} above \code{"metadata"} is requested.
}
\examples{
trail <- audit_record({
  raw    <- dplyr::as_tibble(mtcars)
  clean  <- dplyr::filter(raw, mpg > 20)
  joined <- dplyr::left_join(clean,
                             data.frame(cyl = c(4, 6, 8)), by = "cyl")
})
print(trail)

}
\seealso{
\code{\link[=audit_source]{audit_source()}}, \code{\link[=audit_start]{audit_start()}}

Other audited execution:
\code{\link[=audit_source]{audit_source()}},
\code{\link[=audit_start]{audit_start()}}
}
\concept{audited execution}
