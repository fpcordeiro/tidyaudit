% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit-ambient.R
\name{audit_start}
\alias{audit_start}
\alias{audit_stop}
\title{Audit an Interactive Session}
\usage{
audit_start(
  name = NULL,
  env = globalenv(),
  watch = "data.frames",
  ignore = NULL,
  level = c("metadata", "sample_hash", "column_hash", "full_hash"),
  keys = NULL,
  numeric_summary = TRUE
)

audit_stop()
}
\arguments{
\item{name}{Optional trail name. If \code{NULL}, a timestamped name is generated.}

\item{env}{Environment to watch. Defaults to the global environment.}

\item{watch}{Either \code{"data.frames"} (the default — track every data.frame)
or a character vector of object names to restrict tracking to.}

\item{ignore}{Optional character vector of regular expressions; objects whose
names match any pattern are skipped (e.g. scratch variables).}

\item{level}{Evidence level: \code{"metadata"} (default, privacy-safe) detects
shape/type/NA changes only; \code{"sample_hash"}, \code{"column_hash"}, and
\code{"full_hash"} additionally detect value-only changes by hashing data with a
per-run salt. Salted hashes are \emph{not} a privacy guarantee. The hashing
policy (algorithm, sampling, salt) is recorded in each snapshot's
\code{evidence} field.}

\item{keys}{Optional named list mapping object names to key column(s), used by
the HTML report to flag primary-key status.}

\item{numeric_summary}{Logical; passed to the snapshot builder. If \code{FALSE},
skip numeric quantile summaries (the main cost control on wide data).}
}
\value{
\code{\link[=audit_start]{audit_start()}} returns the new \code{\link[=audit_trail]{audit_trail()}} invisibly;
\code{\link[=audit_stop]{audit_stop()}} returns the completed trail.
}
\description{
Begins ambient capture in an \strong{interactive session} (or a script run
directly with \verb{Rscript file.R}): registers a top-level task callback that
records a snapshot after each statement you run, until \code{\link[=audit_stop]{audit_stop()}}.
}
\details{
This is a convenience wrapper, not the canonical script runner. Task
callbacks fire per top-level statement at the REPL, but R treats
\code{source("file.R")} as a \strong{single} task — so running a script via \code{source()}
under \code{audit_start()} records only one combined step. \strong{For scripts, use
\code{\link[=audit_source]{audit_source()}}.}

The capture handler only \emph{observes} completed statements; it never
re-evaluates them, so side effects are not duplicated. Capture errors are
swallowed so they can never break your REPL.
}
\examples{
\dontrun{
audit_start("session")
raw   <- dplyr::as_tibble(mtcars)
clean <- dplyr::filter(raw, mpg > 20)
trail <- audit_stop()
print(trail)
}

}
\seealso{
\code{\link[=audit_source]{audit_source()}}, \code{\link[=audit_record]{audit_record()}}

Other audited execution:
\code{\link[=audit_record]{audit_record()}},
\code{\link[=audit_source]{audit_source()}}
}
\concept{audited execution}
